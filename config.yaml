# Ausgabepfad für generierte XML-Dateien und Reports
output_path: "./output"

# Pfad zum XSD-Schema SPS (relativ zum Projektroot)
xsd_path: "schemas/pain.001/pain.001.001.09.ch.03.xsd"

# Pfad zum CBPR+ XSD (optional, proprietaer, nicht im Repo)
cbpr_xsd_path: "docs/specs/pain.001/cbpr+nonpublic/CBPRPlus_SR2026_(Combined)_CBPRPlus-pain_001_001_09_FIToFICustomerCreditTransferInitiation_20260320_0758_iso15enriched.xsd"

# Seed für reproduzierbare Zufallsdaten (optional, null = zufällig)
seed: null

# Report-Format: "docx" oder "txt"
report_format: "docx"

# Pfad zum SWIFT BIC Directory (CSV oder JSON, optional)
# Wenn gesetzt, werden BICs gegen dieses Verzeichnis validiert (BR-BIC-001)
# bic_directory_path: "data/swift_bic_directory.csv"
